<UserControl x:Class="moddingSuite.View.Ndfbin.ValueEditing.BlobEditingTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu>
            <MenuItem Header="Export">
                <MenuItem Command="{Binding Value.ExportKdTreeCommand}" Header="Export Kd tree data" />
                <MenuItem Command="{Binding Value.ExportRawCommand}" Header="Export raw" />
            </MenuItem>
        </Menu>
    </Grid>
</UserControl>
